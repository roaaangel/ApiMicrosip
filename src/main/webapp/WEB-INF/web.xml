<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">

    <display-name>ApiMicrosip</display-name>

    <!-- SESSION -->
    <session-config>
        <session-timeout>30</session-timeout>
    </session-config>

    <!-- ✅ CORS FILTER (TU IMPLEMENTACIÓN) -->
    <filter>
        <filter-name>CorsFilter</filter-name>
        <filter-class>com.app.cors.CorsFilter</filter-class>
    </filter>

    <filter-mapping>
        <filter-name>CorsFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

    <!-- ✅ JERSEY 3 (JAKARTA) -->
    <servlet>
        <servlet-name>JerseyServlet</servlet-name>
        <servlet-class>org.glassfish.jersey.servlet.ServletContainer</servlet-class>

        <!-- paquete donde están tus endpoints -->
        <init-param>
            <param-name>jersey.config.server.provider.packages</param-name>
            <param-value>com.app</param-value>
        </init-param>

        <load-on-startup>1</load-on-startup>
    </servlet>

    <servlet-mapping>
        <servlet-name>JerseyServlet</servlet-name>
        <url-pattern>/servicios/*</url-pattern>
    </servlet-mapping>

</web-app>