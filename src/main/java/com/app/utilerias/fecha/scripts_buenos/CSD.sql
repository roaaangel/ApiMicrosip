CREATE TABLE CSD (
    ID          INTEGER GENERATED BY DEFAULT AS IDENTITY,
    RUTA_CER    VARCHAR(100),
    RUTA_KEY    VARCHAR(100),
    PASSWORD  VARCHAR(20),
    RUTA_XMLS   VARCHAR(100)
);

ALTER TABLE CSD ADD CONSTRAINT PK_CSD PRIMARY KEY (ID);