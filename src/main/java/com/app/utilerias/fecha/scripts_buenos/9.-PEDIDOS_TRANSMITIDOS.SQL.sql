CREATE TABLE PEDIDOS_TRANSMITIDOS (
  ID          INTEGER GENERATED BY DEFAULT AS IDENTITY,  
  UUID VARCHAR(100) CHARACTER SET NONE COLLATE NONE,
  DOCTO_VE_ID INTEGER,
  FECHA DATE,
  FOLIO VARCHAR(9)
);


ALTER TABLE PEDIDOS_TRANSMITIDOS ADD CONSTRAINT PK_PEDIDOS_TRANSMITIDOS PRIMARY KEY (ID);
